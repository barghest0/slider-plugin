import {SliderParams} from "./interfaces";

const DEFAULT_SLIDER_PARAMS = {
    min: 0,
    max: 100,
    step: 10,
    value: [0],
    isRange: false,
    direction: "horizontal",
    hasFill: true,
    hasTips: true,
    hasScale: true,
    isDecimal: false,
    decimalPlaces: 0,
};

export { DEFAULT_SLIDER_PARAMS };
