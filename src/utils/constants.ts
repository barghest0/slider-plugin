const DEFAULT_SLIDER_PARAMS = {
	min: 0,
	max: 100,
	step: 10,
	value: [0],
	isRange: false,
	direction: 'horizontal',
	hasFill: true,
	hasTips: true,
	hasScale: true,
	isDecimal: false,
	decimalPlaces: 0,
};

const FIRST_THUMB_STANCE = 0;

const SECOND_THUMB_STANCE = 1;

const FIRST_VALUE = 0;

const SECOND_VALUE = 1;

const FIRST_OFFSET = 0;

const SECOND_OFFSET = 1;

const MAX_OFFSET = 100;

const MIN_OFFSET = 0;

const PARENT_CLASS = 'slider-parent';

const MAIN_CLASS = 'slider';

const TRACK_CLASS = 'slider__track';

const THUMB_CLASS = 'slider__thumb';

const FILL_CLASS = 'slider__fill';

const SCALE_CLASS = 'slider__scale';

const TIP_CLASS = 'slider__tip';

const MIN_CLASS = 'min-value';

const MAX_CLASS = 'max-value';

const FIRST_VALUE_CLASS = 'first-value';

const SECOND_VALUE_CLASS = 'second-value';

const STEP_CLASS = 'step';

const DECIMAL_PLACES_CLASS = 'decimal-places';

const IS_RANGE_CLASS = 'is-range';

const IS_VERTICAL_CLASS = 'is-vertical';

const HAS_SCALE_CLASS = 'has-scale';

const HAS_FILL_CLASS = 'has-fill';

const HAS_TIPS_CLASS = 'has-tips';

const IS_DECIMAL_CLASS = 'is-decimal';

export {
	DEFAULT_SLIDER_PARAMS,
	FIRST_THUMB_STANCE,
	SECOND_THUMB_STANCE,
	FIRST_VALUE,
	SECOND_VALUE,
	MAX_OFFSET,
	MIN_OFFSET,
	FIRST_OFFSET,
	SECOND_OFFSET,
	PARENT_CLASS,
	MAIN_CLASS,
	TRACK_CLASS,
	THUMB_CLASS,
	FILL_CLASS,
	SCALE_CLASS,
	TIP_CLASS,
	MIN_CLASS,
	MAX_CLASS,
	STEP_CLASS,
	SECOND_VALUE_CLASS,
	FIRST_VALUE_CLASS,
	HAS_SCALE_CLASS,
	IS_VERTICAL_CLASS,
	IS_RANGE_CLASS,
	HAS_FILL_CLASS,
	DECIMAL_PLACES_CLASS,
	IS_DECIMAL_CLASS,
	HAS_TIPS_CLASS,
};
