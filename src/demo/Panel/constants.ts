const PANEL_SELECTOR = '.js-slider-panel';
const CHECKBOX_PANEL_SELECTOR = '.js-checkbox-panel';

const MIN_SELECTOR = '[data-param="min-value"]';
const MAX_SELECTOR = '[data-param="max-value"]';
const FIRST_VALUE_SELECTOR = '[ data-param="first-value" ]';
const SECOND_VALUE_SELECTOR = '[data-param="second-value"]';
const STEP_SELECTOR = '[data-param="step"]';
const IS_RANGE_SELECTOR = '[data-param="is-range"]';
const IS_VERTICAL_SELECTOR = '[data-param="is-vertical"]';
const HAS_SCALE_SELECTOR = '[data-param="has-scale"]';
const HAS_FILL_SELECTOR = '[data-param="has-fill"]';
const HAS_TIPS_SELECTOR = '[data-param="has-tips"]';
const CAN_THUMB_PUSH_SELECTOR = '[data-param="can-thumb-push"]';
const CHECKBOX_TYPE = 'checkbox';
const NUMBER_TYPE = 'number';

const TEXT_PARENT_CLASS = 'text-field';
const CHECKBOX_PARENT_CLASS = 'checkbox';

export {
  MIN_SELECTOR,
  MAX_SELECTOR,
  TEXT_PARENT_CLASS,
  CHECKBOX_PARENT_CLASS,
  STEP_SELECTOR,
  PANEL_SELECTOR,
  CHECKBOX_TYPE,
  NUMBER_TYPE,
  SECOND_VALUE_SELECTOR,
  FIRST_VALUE_SELECTOR,
  HAS_SCALE_SELECTOR,
  IS_VERTICAL_SELECTOR,
  IS_RANGE_SELECTOR,
  HAS_FILL_SELECTOR,
  HAS_TIPS_SELECTOR,
  CHECKBOX_PANEL_SELECTOR,
  CAN_THUMB_PUSH_SELECTOR,
};
