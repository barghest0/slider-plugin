include pug-components/slider-example/slider-example.pug 
include pug-components/slider-panel/slider-panel.pug

mixin panel({ instance = 1, isInline = false } = {})
  .panel 
    +slider-example({ instance, isInline })
    if !isInline
      +slider-panel


