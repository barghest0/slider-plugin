import Presenter from 'components/Presenter';

import { SliderParams } from 'components/Slider/types';

function getSliderParams(this: Presenter, params: SliderParams) {
  return params;
}

export default getSliderParams;
